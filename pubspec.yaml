name: date_range_common
description: DateRange class for the Date Range Picker package (the point is to allow to use the same data type between AngularDart and Flutter)
version: 0.1.0
homepage: 'https://github.com/GaspardMerten/date_range'

environment:
  sdk: ">=2.1.0 <3.0.0"
